import AuthPlaceholder from "../public/AuthPlaceholder";

export default function Settings() {
  return <AuthPlaceholder title="Settings" subtitle="Manage preferences and notification controls." />;
}
