import AuthPlaceholder from "../public/AuthPlaceholder";

export default function Fees() {
  return <AuthPlaceholder title="Fee Payment" subtitle="Manage dues, receipts and transaction history." />;
}
