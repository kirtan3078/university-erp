export type Maybe<T> = T | null;

// Default export included as a lightweight placeholder for tooling that expects a module
const typesPlaceholder = {} as Record<string, unknown>;
export default typesPlaceholder;
